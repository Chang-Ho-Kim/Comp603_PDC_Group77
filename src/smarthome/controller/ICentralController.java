package smarthome.controller;

/**
 * ICentralController - Interface for the central application controller.
 * Defines the main contract for the application's main controller.
 */
public interface ICentralController {
    /**
     * Start the main application loop.
     */
    void start();
}
